# F530 #386 corpus sidecar. See test_f530_corpus.py header for the contract.
expected_rule: codex-resume-working-directory-4opt
occurrence: "occ #16 (5th no-fire, 2026-08-29 04:20:31Z) — same palimpsest chooser frame as fixture 02, evaluated against the 4-option rule"
terminal: 7e2ad242
provenance: "https://github.com/chaogebaba/cli-subagents/issues/386#issuecomment-5460305608"
# xfail: SAME real palimpsest frame as fixture 02, but the 4-option rule
# codex-resume-working-directory-4opt requires the option substring
# 'Always use current directory'. On the real frame that option row rendered as
# '7e4.dAlways …' — the zsh prompt bytes overwrote 'Always use current
# directory', so the rule rejects on option[Always use current directory] even
# though the frame classifies WAITING. This is the genuine post-fix no-fire for
# the 4-option variant the issue names (both resume rules 'textually present yet
# no-fire'): the option text was corrupted by the --no-alt-screen palimpsest,
# not truly present in the composited region.
xfail: true
