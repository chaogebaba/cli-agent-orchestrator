# F530 #386 corpus sidecar. See test_f530_corpus.py header for the contract.
expected_rule: codex-resume-workdir-choice
occurrence: "occ #16 (5th no-fire, 2026-08-29 04:20:31Z) — codex resume-cwd chooser composited as a palimpsest with the zsh prompt (codex --no-alt-screen)"
terminal: 7e2ad242
provenance: "https://github.com/chaogebaba/cli-subagents/issues/386#issuecomment-5460305608"
# PASS case (regression guard): the SAME real 7e2ad242.log rendered at the
# frame where the chooser DID composite into the tail — but interleaved with
# leftover shell-prompt bytes (rows 8-9: '3./4. Always …' corrupted by
# '7e…/echo ready'). The fresh control rule codex-resume-workdir-choice needs
# only the title + 'Use session directory' + 'Use current directory', which
# SURVIVED the palimpsest, so it matches and the frame classifies WAITING →
# would fire. Pins that a partially-corrupted chooser is still recoverable for
# the 2-option rule. (Contrast fixture 04, same pane, 4-option rule → xfail.)
xfail: false
