# F530 #386 corpus sidecar. See test_f530_corpus.py header for the contract.
expected_rule: codex-resume-workdir-choice
occurrence: "occ #16 (5th no-fire, 2026-08-29 04:20:16Z) — codex resume-cwd, post-F530-fix build (phoenix-11, c1bacf86 deployed)"
terminal: 7e2ad242
provenance: "https://github.com/chaogebaba/cli-subagents/issues/386#issuecomment-5460305608"
# xfail: this frame is a REAL composited pyte render (cols=80) of the pipe-pane
# byte log ~/.aws/cli-agent-orchestrator/logs/terminal/7e2ad242.log, taken at an
# eval where the bottom-anchored dialog_region tail held the ZSH SHELL PROMPT +
# the `codex resume …` command line — the resume-cwd chooser had NOT yet
# composited into the 20-row tail. Matches the live decisions-log window for
# this terminal (reason=no_rule_matched, window='7e2ad242 on … via 🐍 …'). The
# expected rule rejects on question(contains) because the chooser title is
# absent from the region. This is the DOMINANT live no-fire shape.
xfail: true
