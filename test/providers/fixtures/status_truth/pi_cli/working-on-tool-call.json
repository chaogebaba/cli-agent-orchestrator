{
  "provider": "pi_cli",
  "state": "working",
  "source": "source-derived from working-1.txt: only the working-row message+tail was substituted, preserving the real capture's verbatim ANSI colouring, composer chrome, footer and byte structure.",
  "derivation": "pi 0.85.1 interactive-mode.js:1906 setWorkingMessage('Working on tool call'), extension-set — callable by any loaded extension, and CAO launches pi with --mcp-config (extensions on): '── \u2839 Working on tool call \u2500\u2500\u2500\u2500'.",
  "notes": "Opus r3 EMPIRICAL-GATE-NO: the r3 '$'-tail regressed this live row from PROCESSING to UNKNOWN. r4 closing-rule tail must classify it PROCESSING (raw + ANSI-stripped)."
}
