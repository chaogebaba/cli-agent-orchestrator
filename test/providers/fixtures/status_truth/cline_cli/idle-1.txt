

cli-subagents on  quirks-merge-train [!⇡] via 🐍 v3.14.7 on ☁️  usr@example.invalid
❯

cli-subagents on  quirks-merge-train [!⇡] via 🐍 v3.14.7 on ☁️  usr@example.invalid
❯ >....
`orchestrator/MISTAKES.md` (headers), `ls orchestrator/blueprints/`, `ls scripts/`,
`ls /data/cao-scratch/*gate-report*.md` (gate reports live outside the repo). Then send_message to your caller:
"SECRETARY READY" + one line on anything marked pending in HANDOFF'"'"'s live ledger.
Then idle and wait for queries.

## Events feed (mechanized — you no longer reconcile it)

The `recent` feed at `/data/cao-scratch/fleet-events.log` is maintained by
`scripts/fleet-events-sync.sh` (F481 #336), invoked automatically by the fleet
TUI. You do NOT reconcile it. Your append carve-out remains ONLY for an
explicit user/supervisor-requested note, and any such append still takes the
flock on `/data/cao-scratch/fleet-events.log.lock` first (lock-before-read).


## Answer contract

1. **Terse.** Fact first, then `file:line` citation. No essays, no restating the question.
2. **Re-read before answering.** Your ingest is an index, not the truth — docs
   change mid-session. Re-open the cited section before every answer.
3. **NOT FOUND over guessing.** If you can'"'"'t find it, say NOT FOUND and name the
   2-3 places you looked. Never fabricate a citation.
4. **Quote exactly.** Line-numbered reads (`rg -n`, `nl -ba`); snippets ≤40 lines
   inline, larger extracts → name the file and line range instead of pasting.
5. **Typical queries**: "which script owns X?", "what does exit code N mean in Y?",
   "is there an existing tip/decision about Z?", "which issues are in milestone W?"
   (`gh issue list -R chaogebaba/cli-subagents ...`, read-only), "quote decision Dn
   from blueprint B".

## Task completion

- Spawned via `assign` (park_warm): there is no one-shot deliverable — send the
  READY line, then answer each query with its own send_message to the caller
  (cao-mcp-server **send_message** MCP tool, never a built-in collaboration tool;
  omit `receiver_id` to route to the recorded caller).

## Available Skills

The following skills are available exclusively in this CAO orchestration context. To load a skill'"'"'s full content, use the `mcp__cao-mcp-server__load_skill` tool (NOT the Skill command). T
hese skills are not accessible through provider-native skill commands or directories.

- **cao-worker-protocols**: Worker-side callback and completion rules for assigned and handed-off tasks in CAO' --data-dir /data/cao-scratch/cline-home/231f1de1 "$(cat "$_cao_msgfile")"
done

You are the warm READ-ONLY secretary desk for supervisor b93613da (session cao-claude-orch5). Repos: root /home/chao/VScode_projects/cli-subagents and fork /home/chao/VScode_projects/cli-suba
gents/cli-agent-orchestrator; tracker: `gh issue list -R chaogebaba/cli-subagents`. Answer queries with file:line facts, never edit, never grep/find outside the two repos, /data/cao-scratch a
nd ~/.aws/cli-agent-orchestrator; never touch ~/.claude or ~/sudo_passwd.txt. Prefer `firecrawl` for web lookups. FIRST: reply via the cao-mcp-server MCP `send_message` tool (omit receiver_id
) with exactly `SECRETARY-READY mcp=ok` — if that MCP tool is NOT in your toolset, type `SECRETARY-READY mcp=MISSING` into your pane and stop. Then park and wait for queries.

[Assigned by terminal b93613da. When done, send results back to terminal b93613da using the cao-mcp-server send_message MCP tool — never a built-in collaboration.send_message]
[cao-mcp-server__send_message] {"message":"SECRETARY-READY mcp=ok"}
   ⎿ {"content":[{"text":"{\"success\":true,\"message_id\":1953,\"sender_id\":\"231f1de1\",\"receiver_...
[thinking] Ready message sent. Now I should do my ingest per system prompt? The system prompt says on spawn, read and index ORCH_MAP.md etc. Let me do that quickly so I'm prepared, then park.
 Actually the user prompt says "Then park and wait for queries." But my system prompt says ingest on spawn. I'll do a light ingest to be ready, then park. Let me read the key files quickly.
[run_commands] ls /home/chao/VScode_projects/cli-subagents/ | head -50; ls /home/chao/VScode_projects/cli-subagents/orchestrator/ 2>...
   ⎿ AGENTS.md CLAUDE.md cli-agent-orchestrator cli-subagents docs-archive docs-ca... (+4 more)
[run_commands] grep -n -A3 'pending\|PENDING\|live ledger\|Live Ledger' /home/chao/VScode_projects/cli-subagents/orchestrator/HANDOF...
   ⎿ 10:BUILD AUTHORIZED + IN FLIGHT: kiro_dev-3ff35106 (fork worktree, blueprint ... (+5 more)
[thinking] Ingest done. I've sent SECRETARY-READY already. Note my system prompt said to send "SECRETARY READY" + pending line — but the user prompt demanded exactly `SECRETARY-READY mcp=ok`,
 which I did. Now park and wait. I can append a note? Not requested. Just idle now. I should respond to the user turn minimally and park.
Ingest complete (ORCH_MAP, HANDOFF live ledger, GOLDEN-TIPS/MISTAKES/BUGS/WP-BACKLOG headers, scripts/, blueprints/, gate reports in /data/cao-scratch). READY line already delivered to superv
isor b93613da via MCP send_message. Parking for queries.

