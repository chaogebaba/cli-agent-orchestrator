{
  "provider": "cline_cli",
  "state": "error",
  "state_note": "`state` is the #439 incident's live latched state at capture, NOT the expected post-D14 provider verdict for this fixture; see `notes` for the frozen AC1 R2 verdict.",
  "source": "SUPERVISOR-AUTHORED, NOT A LIVE CAPTURE. Surrounding pane text is a verbatim tmux capture of CAO seat cline_dev-165daae4 (2026-08-30T03:35Z, ClinePass TUI, 28s tick run); the `[abort] aborted by another client` line (cline_cli.py:113 ABORT_LINE) was inserted by hand at the position cline prints a run's finishReason=aborted fallback (cline_cli.py:104-111), replacing the answer.",
  "captured_at": "2026-08-30T03:40:00Z",
  "notes": "The P-A probe (wp-status-truth-probes.md) observed ABORT_LINE live on seat adcaa8a8 but never committed bytes. Reproduction attempts on 165daae4 (second cline client on the same session id; Escape; Ctrl-C = interrupt_keys) did not emit the line — Ctrl-C kills the process outright. Dispatcher cmd is `cat` (idle) when this pane is read; stripped line count is under PANE_LIVENESS_TAIL_LINES, so this is the ACCEPTED sub-floor residual R2 (frozen AC1, wp-status-truth.md:235): the floor authority test is NOT the branch under test here. Expected provider verdict: COMPLETED (R2 — a sub-floor, at/under-45-line pane is non-authoritative, so no ERROR is reported and the seat publishes COMPLETED; the abort text is harvested as the reply), asserted by test/providers/test_cline_f582_abort_truth.py::test_abort_1_fixture_is_the_accepted_sub_floor_residual."
}
