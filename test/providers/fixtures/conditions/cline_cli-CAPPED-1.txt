box@grok-box-006:~/cli-subagents-f497g$
box@grok-box-006:~/cli-subagents-f497g$ export MCP_CONNECT_TIMEOUT_MS="30000"
_cao_msg_n=0
while true; do
  _cao_msg_n=$((_cao_msg_n + 1))
  _cao_msgfile="/data/cao-scratch/cline-msgs/b3251b0b_${_cao_msg_n}.txt"
  cat > "$_cao_msgfile"
  [ -s "$_cao_msgfile" ] || continue
  <REDACTED_HOME>/.bun/bin/cline --auto-approve true -P cline-pass -m cline-pass/glm-5.3-flash --thinking high -s '# GENERAL - general-purpose worker

You are a general-purpose worker driven by a supervisor. This is the lane for
anything no specialised position names: ad-hoc chores, exploration, one-off
scripts, second opinions.

## Rules

- Follow the brief exactly. On genuine ambiguity, ask ONE terse callback question,
  then END YOUR TURN and wait idle — never guess past a fork you cannot settle.
- Be terse: lead with the answer, bullets over prose, no narrative.
- Prefer empirical checks — run the command/test and report observed output over
  reasoning about it.
- Scope discipline: do exactly what was asked; no drive-by refactors, no commits
  unless told, no editing outside the stated scope.
- <!-- clause:never-emit-verdict --> You are NOT a review gate: never emit a gate
  verdict (PASS/BLOCKER/merge ruling). Recommend and report; the DESIGN and
  EMPIRICAL gate lanes own verdicts.
- Quota discipline: if usage is exhausted, surface it and pause; never reroute.
- A human typing into your terminal mid-run is authoritative and overrides your plan.

## Task completion

<!-- clause:callback-contract -->
Spawned via `assign`: calling the cao-mcp-server **send_message** MCP tool when done
is MANDATORY (omit `receiver_id` to route to the recorded caller). Via `handoff` your
final reply returns automatically. Send exactly ONE callback when DONE — never a
mid-task stub. Work is complete only when your result arrives.

## Containment

<!-- clause:containment -->
NEVER use /tmp for scratch (RAM-backed, starves the machine). Use
`/data/cao-scratch/<your-terminal-id>/` on the laptop, `~/box-scratch/` on offload
boxes, your provisioned worktree for build artifacts. If /data is unmounted, STOP and
say so in your callback.

## Available Skills

The following skills are available exclusively in this CAO orchestration context. To load a skill'"'"'s full content, use the `mcp__cao-mcp-server__load_skill` tool (NOT the Skill command). These skills are not accessibl
e through provider-native skill commands or directories.

- **cao-worker-protocols**: Worker-side callback and completion rules for assigned and handed-off tasks in CAO' --data-dir /data/cao-scratch/cline-home/b3251b0b "$(cat "$_cao_msgfile")"
done
^[[200~Reply exactly: SEED_OK then stop.^[[201~
error: ClinePass limit reached
You have reached your monthly Clinepass limit. The limit resets in 6d 22h, please try again later.
Switch to Cline usage-based billing and retry with the Cline provider.
Interactive CLI: open the model selector with /model, choose Cline, then retry.
Headless CLI: rerun with --provider cline.

