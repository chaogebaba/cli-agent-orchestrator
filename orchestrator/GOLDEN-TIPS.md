
## Worker checkout discipline drifts under kiro lanes (2026-08-14, status: queued)
Two same-day incidents: kiro_reviewer-295608f6 answered a scratch-worktree correction by `git checkout`-ing the gate branch in the MAIN fork checkout (restored after, but races any concurrent lane); kiro_dev-1b07df96 (family sweep) was dispatched `use_worktree=true` yet committed f86a0ac6 directly onto the deploy branch fx193-nudge-discipline. Damage was zero both times (tests-only commit; checkout restored) but either could have corrupted a live deploy branch mid-P0. Supervisor countermeasure until mechanized: on EVERY worker callback, verify not just `git branch --show-current` in main checkouts (F201) but also WHERE the reported sha lives (`git branch --contains <sha>`) before treating the artifact as isolated.
- **F207 (feature ask)**: CAO worktree isolation should be enforced, not advisory — spawn worktree workers with a pre-created branch + a repo-level guard (e.g. pre-commit hook installed in the worktree refusing commits on non-`cao/<id>` branches, and read-only main checkout for worker UIDs or a `core.hooksPath` deny on checkout in the primary). Encode, don't document.
